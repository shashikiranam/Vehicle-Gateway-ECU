# Drivers

This directory contains STM32 HAL drivers used by the firmware.

Drivers include support for:

- GPIO
- ADC
- UART
- RCC
- DMA (Future)
- Timer (Future)

These drivers are generated and maintained using STM32CubeMX.

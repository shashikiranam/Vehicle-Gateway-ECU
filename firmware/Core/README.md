# Core

The Core directory contains the main application source files generated by STM32CubeMX along with the application entry point.

Typical contents include:

- main.c
- startup code
- interrupt handlers
- HAL initialization
- system clock configuration

The application logic interfaces with the drivers located in the Src and Inc directories.
